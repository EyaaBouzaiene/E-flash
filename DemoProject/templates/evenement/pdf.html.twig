<head>
    <style>
        /**
            Set the margins of the page to 0, so the footer and the header
            can be of the full height and width !
         **/
        @page {
            margin: 0cm 0cm;
        }
        /** Define now the real margins of every page in the PDF **/
        body {
            margin-top: 3.5cm;
            margin-left: 1cm;
            margin-right: 1cm;
            margin-bottom: 2cm;
        }
        /** Define the header rules **/
        header {
            position: fixed;
            top: 0cm;
            left: 0cm;
            right: 0cm;
            height: 3cm;
        }
        /** Define the footer rules **/
        footer {
            position: fixed;
            bottom: 0cm;
            left: 0cm;
            right: 0cm;
            height: 2cm;
        }
    </style>
</head>
<body>
<!-- Define header and footer blocks before your content -->
<header>
    <div class="intro_price" style="color: purple;margin-left: 10px;margin-top: 10px"><b>E-flash</b></div>
    <div style="font-size: 15px;margin-left: 10px">  Address : Ariana,Tunis </div>
    <div style="font-size: 15px;margin-left: 10px">  Phone number : +216 22665544</div>
    <div style="font-size: 15px;margin-left: 10px">  E-mail : eflash.esprit@gmail.com</div>

    <div><img src="{{ ('uploads/h.png') }}" style="width: 200px;height: 110px;margin-left:750px;margin-top: -80px"></div>
    <hr class="cmDEY eufYP cDHZq" style="color: dimgray;width: 750px;">
</header>

<footer>
</footer>



{% block body %}
<br>
<br>
<div class="container" style="background-color:transparent">
    <h1 align="center" style="color: black">Event List</h1>

    <table align="center" class="theme_dark" style="background-color: #ffffff" style="table-border: 2px" border="" >
        <tr style="background-color: rgba(179,139,194,0.56)">

            <td>id</td>
            <td>Nom</td>
            <td>description</td>
            <td>Duree</td>
            <td>nombrePlace</td>
            <td>lieu</td>
            <td>lon</td>
            <td>lat</td>
            <td>date debut</td>

            <td>image</td>
        </tr>


        </tr>
        {% for evenement in users %}
            <tr>
                <td>{{ evenement.id }}</td>
                <td>{{ evenement.nom }}</td>
                <td>{{ evenement.description }}</td>
                <td>{{ evenement.duree }}</td>
                <td>{{ evenement.nombrePlace }}</td>
                <td>{{ evenement.lieu }}</td>
                <td>{{ evenement.lon }}</td>
                <td>{{ evenement.lat}}</td>

                <td>{{ evenement.dateDebut ? evenement.dateDebut|date('Y-m-d') : '' }}</td>
                <td>{{ evenement.end ? evenement.end|date('Y-m-d') : '' }}</td>
                <td><img src="{{ ('uploads/' ~ evenement.image2) }}" alt="" style="width: 80px" style="height:80px"> </td>






            </tr>
        {% endfor %}
    </table>
    <br><br>
    <div style="margin-left: 450px;font-size: 15px" align="center"> e-flash Team </div>
    <br>
    <div style="margin-left: 550px;font-size: 15px"> Le : {{ "now"|date("d/m/Y")}}  </div>
    <img src="{{ ('uploads/h.png') }}" style="width: 150px;height: 50px;margin-left: 520px" alt="">
</div>



{% endblock %}